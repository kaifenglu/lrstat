% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdsim_seamless.R
\name{rdsim_seamless}
\alias{rdsim_seamless}
\title{Risk Difference Simulation for Phase 2/3 Seamless Design}
\usage{
rdsim_seamless(
  M = 2,
  K = 1,
  rankp0 = 1,
  criticalValues = NA,
  futilityBounds = NULL,
  riskDiffH0s = 0,
  allocations = 1,
  pis = NULL,
  nullVariance = TRUE,
  n = NA,
  plannedSubjects = NA,
  maxNumberOfIterations = 1000,
  seed = 0,
  nthreads = 0
)
}
\arguments{
\item{M}{Number of active treatment arms in Phase 2.}

\item{K}{Number of sequential looks in Phase 3.}

\item{rankp0}{Integer rank in phase 2 used to select the active arm.
\code{rankp0 = 1} selects the most efficacious arm by risk-difference
statistic, \code{rankp0 = 2} selects the second most efficacious arm,
and so on.}

\item{criticalValues}{Numeric vector of length \eqn{K + 1} giving the
critical value for the Wald statistic at each look (Look 1 through
Look \eqn{K + 1}). Decision rule:
\itemize{
\item At Look 1, compute the Wald statistic for each active arm versus the
common control. If the \code{rankp0}-th largest test statistic
exceeds the Look 1 critical value, stop for efficacy.
\item If the Look 1 stopping rule is not met, select the active arm with the
\code{rankp0}-th largest Wald statistic and continue with that arm
only versus control at subsequent looks.
\item For each look \eqn{j = 2,\ldots,K+1}, compare the selected arm to
control; if its Wald statistic exceeds the Look \eqn{j} critical
value, stop for efficacy; otherwise continue.
\item If no critical value is exceeded by Look \eqn{K + 1}, the procedure
ends without rejection.
}}

\item{futilityBounds}{Numeric vector of length \eqn{K} giving the futility
boundaries for Phase 2 and the first \eqn{K-1} looks in Phase 3.
The study stops for futility:
\itemize{
\item in Phase 2 if the selected treatment arm crosses the phase-2 futility
boundary;
\item in Phase 3 if the selected arm crosses the futility boundary at an
interim look;
If omitted, no interim futility stopping is applied.
}}

\item{riskDiffH0s}{Numeric vector of length \eqn{M}. Risk differences
under \eqn{H_0} for each active arm versus the common control. Defaults
to 0 for superiority tests.}

\item{allocations}{Integer or integer vector of length \eqn{M + 1}.
Number of subjects per arm within a randomization block. A single value
implies equal allocation; defaults to 1.
The first \eqn{M} elements refer to the active arms
and the last element refers to the common control.}

\item{pis}{Numeric vector of length \eqn{M + 1}. Each element corresponds
to the response rate for a treatment arm. The first \eqn{M} elements
refer to the active arms and the last element refers to the common control.}

\item{nullVariance}{Whether to use the variance under the null or the
empirical variance under the alternative.}

\item{n}{Planned total sample size across all active arms and control.}

\item{plannedSubjects}{Numeric vector of length \eqn{K + 1} giving the planned
cumulative sample size at each look.
Each entry refers to the combined number of patients for the first
active arm and the common control.}

\item{maxNumberOfIterations}{Number of Monte Carlo replications.
Defaults to 1000.}

\item{seed}{Random seed for reproducibility.}

\item{nthreads}{Number of threads for parallel simulation.
Use 0 to accept the default RcppParallel behavior.}
}
\value{
An S3 object of class \code{"rdsim_seamless"} with these components:
\itemize{
\item \code{overview}: A list summarizing trial-level results and settings:
\itemize{
\item \code{selectionProb}: Probability of selecting each active arm at
the prespecified rank at the end of phase 2.
\item \code{selectToStage2}: Probability of selecting each active arm
to enter stage 2.
\item \code{selectAnyToStage2}: Probability of selecting any active arm
to enter stage 2.
\item \code{rejectPerStage}: Probability of rejecting the null for each
active arm at each stage.
\item \code{futilityPerStage}: Probability of futility stopping for each
active arm at each stage.
\item \code{cumulativeRejection}: Cumulative probability of rejection by stage.
\item \code{cumulativeFutility}: Cumulative futility stopping probabilities
by stage.
\item \code{numberOfEvents}: Cumulative event counts by stage, including
events from all arms in stage 1 and events from the selected arm
and control in later stages.
\item \code{numberOfSubjects}: Cumulative enrollments by stage.
replications that reached that stage.
\item \code{overallReject}: Overall probability of rejecting the null
by trial end.
\item \code{overallFutility}: Overall probability of stopping for futility
by trial end.
\item \code{expectedNumberOfEvents}: Expected cumulative events at trial end.
\item \code{expectedNumberOfSubjects}: Expected cumulative enrollments
at trial end.
\item \code{criticalValues}: The input critical values for each stage.
\item \code{futilityBounds}: The input futility boundaries for each stage.
\item \code{riskDiffH0s}: The input risk differences under \eqn{H_0}.
\item \code{nullVariance}: Whether to use variance under \eqn{H_0}.
\item \code{numberOfIterations}: Number of simulation iterations performed.
\item \code{n}: Planned total sample size.
\item \code{allocations}: The input allocation ratios.
\item \code{responseRates}: The input response rates for each arm.
\item \code{plannedSubjects}: The input planned cumulative sample size at
each look for the first active arm and the common control combined.
\item \code{M}: Number of active arms in Phase 2.
\item \code{K}: Number of sequential looks in Phase 3.
\item \code{rankp0}: Prespecified rank used for phase-2 arm selection.
}
\item \code{sumdata1}: Data frame summarizing each iteration, stage, and
treatment group:
\itemize{
\item \code{iterationNumber}, \code{stopStage}, \code{stageNumber},
\code{treatmentGroup}, \code{accruals}, \code{events}, \code{phat}.
\item For each stage the final row summarizes the overall study
(all arms combined).
}
\item \code{summdata2}: Data frame summarizing test statistics by iteration,
stage, and active arm:
\itemize{
\item \code{iterationNumber}, \code{selectedArm}, \code{stopStage},
\code{stageNumber}, \code{activeArm},
\code{totalAccruals}, \code{totalEvents},
\code{riskDiff}, \code{vriskDiff}, \code{riskDiffZ},
\code{reject}, \code{futility}.
\item For each active arm, total accruals and events refer to
the combined counts for that arm and the common control at that stage.
}
}
}
\description{
Simulate phase 2/3 seamless design testing for risk difference.
}
\examples{
(sim1 <- rdsim_seamless(
  M = 3,
  K = 1,
  criticalValues = c(8, 2.349),
  futilityBounds = 1.036,
  pis = c(0.22, 0.25, 0.35, 0.20),
  n = 1120,
  plannedSubjects = c(280, 560),
  maxNumberOfIterations = 10000,
  seed = 314159,
  nthreads = 1))

}
\author{
Kaifeng Lu, \email{kaifenglu@gmail.com}
}
